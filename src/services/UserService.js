import axios from "axios";

const USERS_REST_API_URL = 'http://localhost:8080/sign-up';

class UserService {
    postUsers() {
        // axios.post(USERS_REST_API_URL).then(r =>  );
    }
}

export default new UserService();